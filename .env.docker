# Django Settings for Docker
DJANGO_SECRET_KEY=docker-dev-secret-key-change-in-production
DEBUG=True
IN_DOCKER=true

# Database Configuration for Docker
POSTGRES_DB=eventlinez
POSTGRES_USER=eventlinez
POSTGRES_PASSWORD=Texera123@
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Allowed Hosts
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Stripe (dummy values for development)
STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
STRIPE_SECRET_KEY=sk_test_placeholder

# Application Settings
TIME_ZONE=America/Los_Angeles
APP_HOST=http://localhost:8000
EVENTLINEZ_FEE=0.12

# Email (dummy for development)
EMAIL_HOST=localhost
EMAIL_PORT=1025
DEFAULT_FROM_EMAIL=noreply@eventlinez.com
