name: Deploy to Test Environment (DISABLED - Replaced by Docker-based workflow)

# This workflow has been disabled to prevent conflicts
# The new Docker-based test-deploy.yml workflow handles testing and deployment
on:
  # Commented out to disable this workflow
  # push:
  #   branches: [ develop ]
  # pull_request:
  #   branches: [ develop ]
  workflow_dispatch: # Only allow manual triggers for emergency

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false # This will never run
    
    steps:
    - name: Workflow Disabled
      run: |
        echo "This workflow has been disabled."
        echo "Please use the Docker-based test-deploy.yml workflow instead."
        exit 1
