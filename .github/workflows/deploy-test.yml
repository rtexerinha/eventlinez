name: Deploy to Test Environment (DISABLED - Replaced by Docker-based workflow)

# This workflow has been completely disabled to prevent conflicts
# The new Docker-based test-deploy.yml workflow handles testing and deployment
on:
  # All triggers disabled to prevent this workflow from running
  workflow_dispatch: # Only allow manual triggers for emergency documentation

jobs:
  # This job is completely removed to prevent any execution
  documentation:
    if: false # Ensures this never runs
    runs-on: ubuntu-latest
    
    steps:
    - name: Workflow Information
      run: |
        echo "ℹ️ This workflow has been replaced by test-deploy.yml"
        echo "📋 The new workflow uses Docker for better consistency"
        echo "🚀 All deployments should use the active test-deploy.yml workflow"
